# OPSD PowerDesk: Day-Ahead Forecasting, Anomaly Detection, Live Simulation & Dashboard

This repository implements the OPSD PowerDesk assignment using the
Open Power System Data (OPSD) time series dataset.

## Setup

```bash
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt
